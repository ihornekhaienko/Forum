@{
    ViewBag.Title = "Users";
}

@model ForumApp.ViewModels.Profile.ProfileListModel

<div class="container body-content">
    <div class="row sectionHeader">
        <div class="sectionHeading">Browse Users</div>
    </div>
    <div class="row" id="channelIndexContent">
        <table class="table table-hover" id="channelIndexTable">
            <thead>
                <tr>
                    <th>
                        Profile Pic
                    </th>
                    <th>
                        Username
                    </th>
                    <th>
                        Is Active
                    </th>
                    <th>
                        Member Since
                    </th>
                    <th>
                        E-mail
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var profile in Model.Profiles)
                {
                    <tr class="userRow">
                        <td>
                            <div class="userProfilePic" style="background-image: url(@profile.ProfileImageLink); background-size:100%"></div>
                        </td>
                        <td>
                            <a asp-controller="Profile" asp-action="Detail" , asp-route-id="@profile.UserId">
                                @profile.UserName
                            </a>
                        </td>
                        <td>
                            @if (profile.IsActive)
                            {
                                <span>Active</span>
                            }
                            else
                            {
                                <span class="inactiveUser">Inactive</span>
                            }
                        </td>
                        <td>
                            @profile.CreateAt
                        </td>
                        <td>
                            @profile.Email
                        </td>
                    </tr>
                }

            </tbody>
        </table>
    </div>
</div>